# Host-only. Package distribution name is tig-silico; CLI entry point is still `silico`.
# Do not `pip install silico` (unrelated package on PyPI).
#
# Default pin is @main. Do not invent a git tag from the package version
# (e.g. pyproject version 0.1.4 does not imply tag v0.1.4 exists).
# Tags through v0.1.3 declare name=silico and fail as tig-silico@v0.1.3.
# Prefer an immutable tag only after one is actually cut with name=tig-silico.
tig-silico @ git+https://github.com/tig/silico.git@main
pytest>=8
# mpy-cross ABI: Silico owns the pin. After the board talks:
#   silico inspect --port COMx --apply-mpy-pin
# Plate default is a starting point only.
mpy-cross==1.27.0.post2
