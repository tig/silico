# Host-only. Package distribution name is tig-silico; CLI entry point is still `silico`.
# Do not `pip install silico` (unrelated package on PyPI).
#
# Pin: editable local clone as a sibling of this GCU (../silico).
# first ship: agent clones tig/silico beside the GCU if missing, then pip install -e.
# CI: plate .github/workflows/ci.yml checks out GCU + silico as siblings so
#     `pip install -r requirements-dev.txt` works unchanged — do not invent a
#     separate silico-src path or skip this file on the runner.
# Do not use git+https://…@… for bootstrap. Do not invent tags from package version.
-e ../silico
pytest>=8
# mpy-cross ABI: Silico owns the pin. After the board talks:
#   silico inspect --port COMx --apply-mpy-pin
# Plate default is a starting point only.
mpy-cross==1.27.0.post2
